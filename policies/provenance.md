# Provenance policy

Originals are read-only. Each derived reading copy or transcript records its source
path, full SHA-256, extraction method and generation time. Exact duplicates share one
canonical reading operation. Similar names with different hashes remain distinct until
reviewed. Summaries and indexes guide retrieval but never replace authoritative source
checks where fidelity matters.
